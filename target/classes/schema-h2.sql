CREATE TABLE IF NOT EXISTS tProduto(
    id VARCHAR(30) PRIMARY KEY,
    product VARCHAR(20), 
    quantity INT, 
    price VARCHAR(20), 
    type VARCHAR(20), 
    industry VARCHAR(100), 
    origin VARCHAR(20)
);
